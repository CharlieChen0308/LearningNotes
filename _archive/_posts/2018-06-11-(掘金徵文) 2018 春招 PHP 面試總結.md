---
categories: Interview
description: (掘金徵文) 2018 春招 PHP 面試總結
---

本文來源：[https://juejin.im/post/5adbf3de518825670f7b6ed0#heading-2](https://juejin.im/post/5adbf3de518825670f7b6ed0#heading-2) ，並在此基礎上，加入了我個人的一些 idea 。

文章的目錄架構大致如下：

![][1]

## PHP

### 基礎

**1、session 和 cookie 的區別**

- Session 是在服務端儲存的一個資料結構，用來跟蹤使用者的狀態，這個資料可以儲存在伺服器記憶體、快取、資料庫中

- Cookie 是客戶端儲存使用者資訊的一種機制，用來記錄使用者的一些資訊，也是實現Session 的一種方式。

為什麼需要 session ？因為 http 是無狀態的，所以需要 session 來記錄使用者的狀態

>參考：[COOKIE和SESSION有什麼區別？ - 知乎](https://www.zhihu.com/question/19786827)

**2、PHP7 用了嗎，瞭解哪些新特性**

* PHP7 的效能更高
* `??` 運算子
* `<=>` 組合比較運算子
* define 定義常量陣列
* `:` 宣告返回值型別
* 匿名類，支援用 new class 來例項化一個匿名類
* 閉包多了一個 call 方法

> 參考：[php7 新特性](https://github.com/nnngu/three_year_php_interview/blob/master/07_PHP/10_php7_new_features.md)

**3、魔術常量**

* `__LINE__`：檔案中的當前行號
* `__FILE__`：檔案的完整路徑和檔名
* `__DIR__`：檔案所在的目錄
* `__FUNCTION__`： 函式名稱
* `__CLASS__`：類的名稱
* `__TRAIT__`：Trait 的名稱
* `__METHOD__`：類的方法名
* `__NAMESPACE__`：當前名稱空間的名稱

>參考：[PHP 手冊 - 魔術常量](http://php.net/manual/zh/language.constants.predefined.php)

**4、魔術方法**

```php
__construct()
__destruct()
__call()
__callStatic()
__get()
__set()
__isset()
__unset()
__sleep()
__wakeup()
__toString()
__invoke()   // 當嘗試以呼叫函式的方式呼叫一個物件時，__invoke() 方法會被自動呼叫。
__set_state()
__clone()
__debugInfo()
```

>參考：[PHP 手冊 - 魔術方法](http://www.php.net/manual/zh/language.oop5.magic.php)

## 資料結構和演算法

### 1、棧資料結構

```php
<?php

class Stack {

    private $top = -1;
    private $stack = array(); // 利用陣列儲存資料

    // 入棧
    public function push($ele){
        $this->stack[++$this->top] = $ele; // 此處必須是++i，先計算再使用
    }

    // 出棧，返回出棧元素
    public function pop(){
        if ($this->top == -1){
            echo 'stack is empty...';
            return false;
        }
        $ele = $this->stack[$this->top];
        unset($this->stack[$this->top--]); // 此處必須是i--，先使用再計算（注意出棧和入棧的區別）
        return $ele;
    }

    // 遍歷棧
    public function show(){
        if ($this->top == -1){
            echo 'stack is empty...';
            return false;
        }
        for($i=$this->top; $i>-1; $i--){
            echo $this->stack[$i].'<br/>';
        }
    }

}

```

>參考：[PHP實現棧資料結構](http://www.cnblogs.com/firstForEver/p/5252277.html)

### 2、氣泡排序













  [1]: https://www.github.com/nnngu/FigureBed/raw/master/2018/6/11/1528675027041.jpg